# Changelog

## 0.2.6

- 导入确认：弹窗新增“当前数据 vs 导入数据”对比视图，并显示组合/收藏夹名称预览与数量增减摘要
- 样式：为导入对比视图补充响应式布局，移动端自动切成单列

## 0.2.5

- 恢复备份：菜单新增“恢复备份”，可一键恢复最近一次导入前自动保存的本地数据
- 标签布局：顶部组合/收藏夹标签改回自动换行，不再出现横向滚动条
- 版本同步：更新应用脚本、Manifest 与 Service Worker 版本号

## 0.2.4

- 导入安全性：Markdown 导入新增内容校验、摘要预览、覆盖确认，并在覆盖前自动备份当前本地数据
- 导入清洗：忽略空标题，按容器内去重 TAG，避免无效文件直接清空数据
- Service Worker：仅对导航请求回退到应用壳，避免把 `index.html` 错误返回给脚本、样式和 JSON 资源
- 文档与帮助：同步当前版本号和浏览/编辑模式交互说明

## 0.2.3

- 手动双击追踪：修复编辑模式双击删除（tabs/偶像/TAG）；移除浏览模式不必要的双击事件
- 移动端拖拽优化：降低触发阈值；阻止滚动干扰；增强视觉反馈（放大+阴影）
- Service Worker：修复 chrome-extension:// 缓存报错
- 重命名功能：编辑模式单击当前项延迟重命名（拖拽/双击取消）
- 指针捕获优化：延迟到拖拽开始后，修复 PC 端点击重命名失效

## 0.2.2

- 编辑/浏览逻辑分离：编辑模式禁用复制
- 编辑模式长按重命名：组合/收藏夹/偶像/TAG
- 编辑模式拖拽排序：按住 ≡ 手柄拖拽（tabs/偶像/TAG/收藏TAG）
- PC 端布局优化：容器占用更大宽度；偶像卡片自适应列数

## 0.2.1

- 语言弹窗体验：下拉即切换、编辑/新增后回到语言弹窗、关闭可回退预览
- 优化语言弹窗按钮文案（更短）与 CJK 换行显示
- PWA 缓存版本更新

## 0.2.0

- i18n：内置多语言 + 可新增/编辑/删除用户自定义语言
- 默认浏览模式 + 右上角“＋/×”切换编辑模式（新增/删除/改色仅编辑模式可用）
- PWA 缓存版本更新

## 0.1.0

- 极简标签页布局：组合 tabs → 偶像卡片 → TAG
- 单击复制、双击删除；组合支持长按复制全部 TAG
- 收藏夹（两层结构）与快速复用已有 TAG
- 颜色选择：预设色板 + HEX 输入
- Markdown 导入/导出
- PWA（离线可用）
